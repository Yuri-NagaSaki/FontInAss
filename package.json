{
  "name": "fontinass-local",
  "version": "2.0.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": ["packages/*", "server", "web"],
  "scripts": {
    "dev": "bun run --cwd server dev & bun run --cwd web dev",
    "typecheck": "bun run --cwd packages/contracts typecheck && bun run --cwd packages/subtitle-processing typecheck && bun run --cwd packages/font-catalog typecheck && bun run --cwd packages/archive-library typecheck && bun run --cwd packages/access-control typecheck && bun run --cwd packages/font-submission typecheck && bun run --cwd packages/activity-log typecheck && bun run --cwd packages/persistence typecheck && bun run --cwd packages/storage typecheck && bun run --cwd server typecheck && bun run --cwd web typecheck",
    "test": "bun test packages server/src",
    "build": "bun run --cwd web build && bun run --cwd server build",
    "check": "bun run typecheck && bun run test && bun run build",
    "data:manifest": "bun server/scripts/export-archive-manifest.ts",
    "data:reindex": "bun server/scripts/rebuild-font-index.ts",
    "start": "bun run --cwd server start"
  },
  "devDependencies": {
    "@types/bun": "^1.2.14",
    "typescript": "^5.8.2"
  }
}
